---
title: MM-algorithms for traditional and convex NMF with Tweedie and Negative Binomial cost functions and empirical evaluation
title_zh: 针对具有 Tweedie 和负二项代价函数的传统及凸 NMF 的 MM 算法及其实证评估
authors: "Elisabeth Sommer James, Asger Hobolth, Marta Pelizzola"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09601v1"
tags: ["query:ai-ast"]
score: 8.0
evidence: 用于无监督学习和特征提取的非负矩阵分解
tldr: 本研究针对传统非负矩阵分解（NMF）在处理过度离散数据时的局限性，提出了一个统一的框架，涵盖了基于Tweedie和负二项分布代价函数的传统及凸NMF模型。通过大化最小化（MM）算法，推导出了新的乘法更新规则，并提供了R语言实现。实验证明，选择合适的噪声模型对模型拟合至关重要，且凸NMF在大规模类别场景下表现出更强的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的NMF通常假设高斯或泊松噪声，难以有效处理具有过度离散特征或复杂均值-方差关系的实际数据。
method: 利用大化最小化（MM）方法，为包含Tweedie和负二项分布在内的多种分布模型推导出了统一的乘法更新规则。
result: 在突变和词频数据上的实验表明，噪声模型的选择显著影响特征恢复效果，且凸NMF在多类别场景下更具效率。
conclusion: 该研究扩展了NMF的应用范围，提供的统一框架和开源工具为处理复杂分布数据提供了稳健的特征提取方案。
---

## 摘要
非负矩阵分解 (NMF) 是一种广泛用于无监督学习和特征提取的工具，应用范围涵盖从基因组学到文本分析和信号处理。NMF 的标准公式通常是在高斯或泊松噪声假设下推导出来的，这对于表现出过度离散或其他复杂均值-方差关系的数据可能并不适用。在本文中，我们为广泛的分布假设（包括负二项和 Tweedie 模型）下的传统和凸 NMF 开发了一个统一框架，并强调了 Tweedie 与 β-散度之间的联系。利用 MM 算法 (Majorize-Minimization) 方法，我们为所有考虑的模型推导了乘性更新规则，并为具有泊松和负二项代价函数的凸 NMF 提出了新的更新规则。我们提供了所有考虑模型的统一实现，包括几种凸 NMF 模型的首次实现。对突变和词频数据的实证评估表明，噪声模型的选择会关键性地影响模型拟合和特征恢复，并且在类别数量较多的情况下，凸 NMF 可以作为传统 NMF 的一种高效且稳健的替代方案。我们提出的更新代码可在 R 包 nmfgenr 中获取，网址为 https://github.com/MartaPelizzola/nmfgenr。

## Abstract
Non-negative matrix factorisation (NMF) is a widely used tool for unsupervised learning and feature extraction, with applications ranging from genomics to text analysis and signal processing. Standard formulations of NMF are typically derived under Gaussian or Poisson noise assumptions, which may be inadequate for data exhibiting overdispersion or other complex mean-variance relationships. In this paper, we develop a unified framework for both traditional and convex NMF under a broad class of distributional assumptions, including Negative Binomial and Tweedie models, where the connection between the Tweedie and the $β$-divergence is also highlighted. Using a Majorize-Minimisation approach, we derive multiplicative update rules for all considered models, and novel updates for convex NMF with Poisson and Negative Binomial cost functions. We provide a unified implementation of all considered models, including the first implementations of several convex NMF models. Empirical evaluations on mutational and word count data demonstrate that the choice of noise model critically affects model fit and feature recovery, and that convex NMF can provide an efficient and robust alternative to traditional NMF in scenarios where the number of classes is large. The code for our proposed updates is available in the R package nmfgenr and can be found at https://github.com/MartaPelizzola/nmfgenr.

---

## 论文详细总结（自动生成）

这是一份关于论文《MM-algorithms for traditional and convex NMF with Tweedie and Negative Binomial cost functions and empirical evaluation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
非负矩阵分解（NMF）是无监督学习中提取特征的常用工具。然而，传统的 NMF 算法通常基于高斯（正态）或泊松分布假设。在处理实际数据（如癌症基因突变计数或文本词频）时，数据往往表现出**过度离散（Overdispersion）**、重尾分布或复杂的均值-方差关系，此时传统假设会导致模型拟合偏差和特征提取失真。
本文的核心动机是建立一个统一的数学框架，将更具灵活性的 **Tweedie 分布**和**负二项（Negative Binomial）分布**引入传统 NMF 和凸 NMF（Convex NMF）中，以解决复杂噪声环境下的特征提取问题。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用**大化最小化（Majorize-Minimization, MM）算法**框架，为不同概率分布下的代价函数推导闭式乘性更新规则（Multiplicative Update Rules）。
*   **关键技术细节**：
    *   **统一框架**：涵盖了传统 NMF ($V \approx WH$) 和凸 NMF ($V^T \approx V^T ED$)。在凸 NMF 中，特征被限制为原始数据的线性组合，具有更好的解释性。
    *   **分布扩展**：
        *   **Tweedie 分布**：通过参数 $p$ 统一了正态 ($p=0$)、泊松 ($p=1$)、Gamma ($p=2$) 和逆高斯 ($p=3$) 等分布，并建立了其与 $\beta$-散度的联系。
        *   **负二项分布**：专门用于处理方差远大于均值的计数数据。
    *   **算法推导**：论文首次推导了**负二项分布下凸 NMF 的更新规则**。通过构造辅助函数（Auxiliary Function）并利用 Jensen 不等式和对数凹性，确保每次迭代都能降低代价函数值。
    *   **参数估计**：通过剖面似然法（Profile Likelihood）估计 Tweedie 的幂参数 $p$ 和负二项分布的离散参数 $\alpha$。

### 3. 实验设计
论文在两个具有代表性的真实场景中进行了评估：
*   **数据集/场景**：
    1.  **癌症基因组学**：来自 PCAWG 数据库的 260 名肝癌患者的突变计数数据（96 种突变类型）。目标是提取“突变签名”（Mutational Signatures）。
    2.  **文本挖掘**：20 Newsgroups 数据集的子集（500 篇文档，6354 个词汇，涵盖体育、宗教、政治 3 个主题）。目标是进行主题建模。
*   **Benchmark 与对比方法**：
    *   对比了传统 NMF 与 凸 NMF。
    *   对比了四种分布假设：正态、泊松、Tweedie、负二项。
    *   评估指标包括 BIC（贝叶斯信息准则）、残差分析、余弦相似度（与已知标准 COSMIC 签名对比）。

### 4. 资源与算力
*   **硬件环境**：实验在 GenomeDK 集群上运行。
*   **软件实现**：算法使用 **Rcpp** 编写，集成在 R 包 `nmfgenr` 中，以提高计算效率。
*   **算力细节**：文中未明确标注具体的 GPU 型号（NMF 算法通常在 CPU 上运行）。论文提供了运行时间参考：在标准笔记本电脑上，负二项分布参数 $\alpha$ 的估计约需 45 秒，Tweedie 参数 $p$ 的估计约需 3 分钟。凸 NMF 的计算复杂度为 $O(MN^2K)$，高于传统 NMF 的 $O(MNK)$。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同领域的数据集进行了完整的交叉验证和参数扫描（如 $K$ 值的选择、分布参数的拟合）。
*   **充分性评价**：实验设计较为充分。论文不仅对比了模型拟合度（BIC），还通过残差图直观展示了不同分布假设对过度离散现象的处理能力。通过与生物学已知标准（COSMIC）对比，验证了结果的科学客观性。

### 6. 主要结论与发现
*   **噪声模型至关重要**：选择错误的分布（如在过度离散数据上使用泊松分布）会导致明显的残差偏差。
*   **负二项与 Tweedie 的优越性**：在处理生物突变数据时，负二项分布提供了最佳拟合；在处理高度稀疏的文本数据时，Tweedie 分布表现最优。
*   **凸 NMF 的优势**：在类别数量大、数据高度稀疏的场景下，凸 NMF 虽然计算复杂度较高，但由于其参数量相对较少（在特定条件下），能提供比传统 NMF 更稳健、BIC 更低的拟合结果，且具有更强的正则化效果。

### 7. 优点（亮点）
*   **理论完备性**：填补了负二项分布在凸 NMF 框架下的理论空白。
*   **实用性强**：提供了高效的 R 语言工具包，支持大规模数据集。
*   **诊断工具**：强调了残差分析在 NMF 模型选择中的重要性，而不仅仅依赖于重建误差。

### 8. 不足与局限
*   **计算开销**：凸 NMF 的计算复杂度随样本量 $N$ 的平方增长，在大规模数据集（$N$ 极大时）可能面临扩展性瓶颈。
*   **收敛速度**：Tweedie 分布在某些 $p$ 值下涉及复杂的幂运算，导致收敛速度慢于传统的正态或泊松模型。
*   **理论深度限制**：论文主要关注算法推导和实证，对于凸 NMF 在不同噪声下的解的唯一性和可辨识性（Identifiability）缺乏深入的理论探讨。

（完）
