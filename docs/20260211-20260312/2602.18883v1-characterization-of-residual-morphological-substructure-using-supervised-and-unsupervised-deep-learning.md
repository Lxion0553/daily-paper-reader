---
title: Characterization of Residual Morphological Substructure Using Supervised and Unsupervised Deep Learning
title_zh: 利用监督和无监督深度学习表征残差形态子结构
authors: "Kameswara Bharadwaj Mantha, Daniel H. McIntosh, Cody Ciaschi, Rubyet Evan, Luther Landry, Henry C. Ferguson, Camilla Pacifici, Joel Primack, Nimish Hathi, Anton Koekemoer, Yicheng Guo, The CANDELS Collaboration"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18883v1"
tags: ["query:ai-ast"]
score: 10.0
evidence: 用于星系子结构表征的无监督深度学习
tldr: "本研究利用深度学习技术对CANDELS巡天中10,046个大质量星系的残差图像进行形态子结构表征。通过开发监督卷积神经网络（CNN）和无监督卷积变分自编码器（CvAE），结合独特的图像预处理和增强方法，研究探讨了这些模型在识别星系演化物理过程中的潜力。结果表明，监督CNN在区分残差强度方面表现优异，而无监督CvAE虽能捕捉特征但分类能力较弱。"
selection_source: fresh_fetch
motivation: 旨在通过自动化表征星系光度轮廓拟合后的残差子结构，深入理解驱动星系演化的物理过程。
method: 采用监督CNN和无监督CvAE对CANDELS巡天星系的残差图像进行训练，并结合PCA、GMM聚类和多种残差强度指标进行评估。
result: 监督CNN的潜空间特征与残差强度指标高度相关，能有效区分强弱子结构，而无监督CvAE在子结构分类上的判别力有限。
conclusion: 监督学习在星系残差形态分类中具有显著优势，为大规模巡天数据中的星系演化研究提供了有效的自动化工具。
---

## 摘要
星系子结构的自动表征是理解驱动星系演化的变革性物理过程的关键步骤。在本研究中，我们探讨了深度学习（DL）框架在表征不同星系子结构方面的应用，这些子结构存在于 CANDELS 巡天大样本星系的参数化光轮廓减除后的“残差”图像中。我们开发了一个监督卷积神经网络（CNN）和一个无监督卷积变分自编码器（CvAE），并利用 $1<z<3$ 范围内的 $10,046$ 个明亮且大质量星系（$H<24.5\,{\rm mag}$ 且 $M_{\rm stellar} \geq 10^{9.5}\,M_{\odot}$）的单 Sérsic 轮廓拟合残差图像对其进行训练，同时结合了指示其内部残差子结构性质的视觉分类标签。利用我们独特的数据预处理方法，我们对残差图像进行处理，使深度学习网络的输入仅包含“目标星系”，并对其进行增强，使样本在不同的残差特征上均匀分布。我们利用主成分分析（PCA）以及独立量化的残差强度指标（显著像素通量 $SPF$、颠簸度 Bumpiness 和残差通量占比 Residual Flux Fraction）评估了 CNN 和 CvAE 的潜在空间。我们还采用了基于无监督高斯混合模型（GMM）的聚类方案和支持向量分类（SVC），以识别 PCA 空间中对应于相似残差子结构的分组。我们发现，监督 CNN 在 PCA 空间中的潜在特征与 $SPF$ 值相关，并能区分定性上的强残差和弱残差子结构。虽然无监督 CvAE 的潜在空间也与视觉和定量残差特征相关，但在表征不同残差子结构时缺乏明确的判别能力。

## Abstract
Automated characterization of galactic substructure is an essential step in understanding the transformative physical processes driving galaxy evolution. In this study, we investigate the application of deep learning (DL) frameworks to characterize different galactic substructures hosted within parametric light-profile subtracted ``residual'' images of a large sample galaxies from the CANDELS survey. We develop a supervised Convolutional Neural Network (CNN) and unsupervised Convolutional Variational Autoencoder (CvAE) and train it on the single-Sérsic profile fitting based residual images of $10,046$ bright and massive galaxies ($H<24.5\,{\rm mag}$ and $M_{\rm stellar} \geq 10^{9.5}\,M_{\odot}$) spanning $1<z<3$, in conjunction with their visual-based classification labels indicating the nature of residual substructures hosted within them. Using our unique data preprocessing approach, we prepare our residual images such that the inputs to our DL networks comprise only ``galaxy of interest'', and augment them such that our sample span uniformly across different residual characteristics. We assess the latent space of the CNN and CvAE using Principle Component Analysis (PCA) along with independently quantified metrics of residual strength (significant pixel flux $SPF$, Bumpiness, and Residual Flux Fraction). We also employ an unsupervised Gaussian Mixture Modeling (GMM) based clustering scheme with Support Vector Classification (SVC) to identify groupings in PCA space that correspond to similar residual substructure. We find that our supervised CNN latent features in PCA space correlate with the $SPF$ values and distinguish between qualitatively strong and weak residual substructures. While our unsupervised CvAE latent space also correlates with visual and quantitative residual characteristics, but lacks clear discriminatory power when characterizing different residual substructures.

---

## 论文详细总结（自动生成）

这篇论文题为《利用监督和无监督深度学习表征残差形态子结构》，由 Kameswara Bharadwaj Mantha 等人撰写。以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：星系合并是驱动星系演化的核心物理过程，但观测上识别合并（尤其是晚期合并）极具挑战。传统的形态学方法（如 $CAS$、$G-M_{20}$）或视觉分类在处理大规模巡天数据时存在主观性强、效率低、且易受宇宙学表面亮度变暗影响的问题。
*   **核心问题**：如何利用深度学习自动化地表征星系在减去平滑光轮廓（如单 Sérsic 模型）后的“残差图像”中的子结构（如潮汐尾、团块、不对称特征），从而为理解星系演化提供更客观、高效的工具。

### 2. 论文提出的方法论
*   **核心思想**：通过对比监督学习（CNN）和无监督学习（CvAE）在处理残差图像时的表现，探索潜在空间（Latent Space）特征与物理指标之间的关联。
*   **关键技术细节**：
    *   **图像预处理（Object-Only）**：这是本文的一大亮点。研究者通过源提取软件（SEP）生成掩模，仅保留目标星系的像素，背景则用随机采样的纯天空背景填充，消除了相邻天体的干扰。
    *   **监督 CNN**：采用类似 Huertas-Company (2015) 的架构，包含 3 组卷积+池化层，最后接全连接层输出 5 类残差特征的概率。
    *   **无监督 CvAE（卷积变分自编码器）**：通过编码器将图像压缩至 512 维潜在空间，再通过解码器重建。损失函数结合了重建误差（MSE）和 KL 散度（衡量潜在分布与标准正态分布的接近程度）。
    *   **潜在空间分析**：利用主成分分析（PCA）对 512 维特征降维，并结合高斯混合模型（GMM）进行聚类，最后用支持向量机（SVC）确定分类边界。
*   **定量指标**：引入了显著像素通量（$SPF$）、颠簸度（$B$）和残差通量占比（$RFF$）作为独立物理度量。

### 3. 实验设计
*   **数据集**：来自 CANDELS 巡天的 10,046 个大质量星系（$1 < z < 3$，$M_* \geq 10^{9.5} M_\odot$）。
*   **基准（Benchmark）**：由三位人类专家视觉分类生成的标签，分为五类：Clean（干净）、General（常规）、Core（核心残差）、Asymmetric（不对称）、Peculiar（奇特/强扰动）。
*   **对比方法**：主要对比了监督 CNN 的分类能力与无监督 CvAE 的特征提取能力，并验证了这些特征是否与 $SPF$ 等物理指标一致。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号或数量。
*   **训练细节**：提到了训练使用了 Adam 优化器，初始学习率为 $10^{-4}$，最大训练轮数为 100 Epochs，Batch Size 为 32。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了约 1 万个星系的真实观测数据。
    *   **数据增强**：通过旋转和翻转将训练集扩充至 25,000 张图像，以平衡各类别样本量。
    *   **验证方式**：采用了 80/20 的训练/测试集划分，并进行了 PCA 降维分析、GMM 聚类实验以及与三种独立物理指标的相关性分析。
*   **充分性评价**：实验设计较为充分，涵盖了从数据预处理到模型训练再到后期统计分析的全流程。但由于视觉标签本身存在主观性，监督学习的上限受限于标签质量。

### 6. 论文的主要结论与发现
*   **监督 CNN 表现优异**：CNN 的潜在空间特征与残差强度指标（尤其是 $SPF$）高度相关，能够清晰地在 PCA 空间中区分出“干净”与“强残差”星系。测试集准确率约为 75%，其中 Clean 类准确率达 91%。
*   **无监督 CvAE 的局限性**：CvAE 虽然能捕捉到残差强度的连续变化（PC1 与 $SPF$ 相关），但在区分具体的形态类别（如 Asymmetric vs. Peculiar）时判别力较弱，特征分布呈连续状态而非离散簇。
*   **聚类发现**：通过 GMM 聚类，CNN 特征空间可以自然地划分为 6 个簇，分别对应不同强度的物理特征，这为自动化筛选具有科学研究价值的合并候选体提供了依据。

### 7. 优点
*   **创新的预处理**：提出的“Object-Only”方法有效解决了深度学习在天文图像中容易受背景噪声和邻近源干扰的痛点。
*   **多维度验证**：不仅依赖模型输出，还通过 PCA 和独立物理指标（$SPF, B, RFF$）对潜在空间进行了深度物理解释。
*   **实用性强**：为未来大规模巡天（如 Euclid, Roman）提供了自动化的残差分析框架。

### 8. 不足与局限
*   **标签主观性**：监督学习高度依赖人类专家的视觉分类，而人类在判断微弱潮汐特征时存在不可避免的偏差。
*   **CvAE 重建模糊**：无监督模型的重建图像存在全局平滑（模糊）现象，导致其对细微、高频的子结构捕捉不足。
*   **类别混淆**：在 Peculiar、General 和 Asymmetric 类之间存在明显的分类混淆，说明模型在区分复杂扰动形态方面仍有提升空间。
*   **算力信息缺失**：未提供训练效率和硬件需求信息，不利于其他研究者评估复现成本。

（完）
